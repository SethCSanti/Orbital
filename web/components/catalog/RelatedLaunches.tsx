import type { RelatedLaunch } from "@/lib/api";

export default function RelatedLaunches({ launches }: { launches: RelatedLaunch[] }) {
  return <section className="mt-8 border border-line bg-panel p-5"><div className="flex items-baseline justify-between gap-4 border-b border-line pb-4"><h2 className="font-display text-xl font-semibold text-ink">Related launch history</h2><span className="text-xs text-dim">{launches.length}{launches.length === 200 ? "+" : ""} launches</span></div>{launches.length === 0 ? <p className="py-6 text-sm text-muted">No related launches have been collected yet.</p> : <div className="divide-y divide-line">{launches.map((launch) => <article key={launch.id} className="grid gap-2 py-4 sm:grid-cols-[minmax(0,1fr)_180px]"><div><h3 className="font-display font-medium text-ink">{launch.name}</h3><p className="mt-1 text-sm text-muted">{launch.missionName || "Mission unlisted"} · {launch.rocketName || "Vehicle unlisted"}</p></div><div className="text-left text-xs text-dim sm:text-right"><p>{new Date(launch.net).toLocaleDateString()}</p><p className="mt-1 text-signal">{launch.statusName}</p></div></article>)}</div>}</section>;
}
